package es.practicatuprogramacion.natiworlds.detectarErrores.nivel1

fun concatenarCadenas(cadena1: String, cadena2: String): String {
    // Encuentra y corrige el error
    return cadena1 + cadena2 + cadena2
}
